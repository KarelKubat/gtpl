package syringe

import "testing"

func TestAll(t *testing.T) {
	// Testing isn't implemented, the doc generation runs the tool which must succeed for a successful `make`.
	// I might add specific smaller-unit tests later.
}
